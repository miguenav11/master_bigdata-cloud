version: 2

models:
  - name: stg__teams
    description: "Master catalog of teams (Clubs and Nations)"
    columns:
      - name: team_id
        description: "Team primary key (PK)"
        tests:
          - unique
          - not_null
      